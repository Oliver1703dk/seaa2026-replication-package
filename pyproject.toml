[project]
name = "architectural-erosion"
version = "1.0.0"
description = "Vibe Coding Meets Architecture: A Causal Study of Agentic AI in Java Repositories (SEAA 2026 STREAM)"
requires-python = ">=3.11,<3.13"
license = {text = "MIT"}
authors = [
    {name = "Oliver Aleksander Larsen", email = "olar@mmmi.sdu.dk"},
    {name = "Mahyar T. Moghaddam", email = "mtmo@mmmi.sdu.dk"},
]

dependencies = [
    "pandas>=2.2",
    "numpy>=1.26",
    "scikit-learn>=1.4",
    "networkx>=3.2",
    "PyGithub>=2.2",
    "pydriller>=2.9",
    "requests>=2.31",
    "pyyaml>=6.0",
    "tqdm>=4.66",
]

[dependency-groups]
# Dev tools (tests, lint, notebooks). uv installs these by default with
# `uv sync` / `uv run`, so `uv run pytest tests/` works out of the box. The
# Docker image uses `uv sync --frozen --no-dev`, which excludes this group.
dev = [
    "pytest>=8.0",
    "ruff>=0.3",
    "ipykernel>=6.29",
    "jupyter>=1.0",
    "matplotlib>=3.8",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
